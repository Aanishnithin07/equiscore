import React from 'react';

export const TeamResultsPage = () => {
    return (
        <div className="w-full min-h-[100dvh] bg-[var(--bg-void)] flex flex-col items-center justify-center p-8">
            <h1 className="heading-1 text-white mb-4">Team Results</h1>
            <p className="text-[var(--text-secondary)]">Your pitch deck has been evaluated.</p>
        </div>
    );
};
